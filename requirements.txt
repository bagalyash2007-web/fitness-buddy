# requirements.txt — Fitness Buddy Python dependencies
# -------------------------------------------------------
# Install all dependencies with:  pip install -r requirements.txt
#
# Render.com uses Python 3.11 — we use ibm-watsonx-ai (requires Python >=3.11).
# For local Python 3.9, use ibm-watson-machine-learning>=1.0.300 instead.

flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
ibm-watsonx-ai>=1.1.2
gunicorn>=21.2.0
